/// @function scr_xy_point_sum(pointA, pointB)
/// @description Given two Point arrays, return the sum of the 2
/// @param {[point.x, point.y]} PointA
/// @param {[point.x, point.y]} PointB

var ax = argument0[point.x];
var ay = argument0[point.y];
var bx = argument1[point.x];
var by = argument1[point.y];
var point = [ax+bx, ay+by];
return point;
